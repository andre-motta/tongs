{
  "name": "tongs-desktop-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/src/main/index.js",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "clean": "node scripts/build.mjs --clean",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "test": "npm run build && node --test ../tests/desktop/electron/*.test.mjs",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "44.2.0",
    "typescript": "7.0.2"
  }
}
